# Final v3 candidate — TESTNET verification only

The exact release candidate is bound to the private v3 manifest and CAR by hashes. Final metadata URLs, trait mappings, rankings and reveal content are not included here.

`candidate.json` is a canonical unsigned mainnet-format package. It is NOT promoted to the production client. `testnet/final/` derives strictly TESTNET (`-3`) wallet requests from it, with test-only destination flags and owner validation. The existing mainnet configuration remains held.

A read-only snapshot downloaded from the page is not transaction-history or media-availability proof. Do not set release approval booleans merely because the page shows a supply increase or emulator tests passed.

A compact private source/metadata recovery backup was uploaded to the owner's Dropbox on 2026-09-17. This is not a pinned public IPFS copy of the 10,000 images. Permanent release publication and external reveal scheduling still need to be completed before claiming operational automatic reveal.

No private wallet key is requested or stored. Mainnet deployment and opening require the owner's own signatures after the complete release checks.
