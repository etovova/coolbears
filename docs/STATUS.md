# CoolBears — Solana / Magic Eden

Активная сборка: solana-20260918. Проект etovova/coolbears, сайт https://coolbears-nfts.com.
Сохранять сайт и оригиналы; исходные слои не менять и не сжимать. Утверждённые тексты находятся в metadata/policy.json. Старые TON-проекты не использовать.

## Продолжение после скриншотов 08:33 — manual-25-check-20260921

На телефоне чтение показывает1950/10000, но ручная загрузка возвращает429. Предыдущее исправление НЕ является подтверждением работы подписи/отправки в Phantom. Точный метод отказа на телефоне пока неизвестен: прежний runUpload скрывал причину за общей строкой. Проверка пяти методов на обоих публичных RPC из среды агента дала HTTP200; переносить этот результат на телефон нельзя.

Убрана лишняя зависимость сверки pending от getSlot+getBlock: getEpochInfo(finalized) возвращает высоту и slot одного банка. Аккаунт читается finalized с minContextSlot; история подписей запрашивается только для отсутствующих пакетов. Уже записанный пакет подтверждается по содержимому машины без getBlock/getSignatureStatuses. Проверки сети, владельца, metadata, срока подписи, сохранение pending до отправки и запрет повторной отправки сохранены. Неопределённый статус не приравнивается к отсутствующему. Новых автоматических циклов нет.

Добавлена ограниченная диагностика последних40 запросов (метод, hostname, HTTP/RPC-код либо таймаут, время). В ней нет тел запросов, подписанных байтов, ключей или URL-параметров. Ошибка доступна в «Подробности ошибки» и «Показать журнал для сохранения». Это нужно для установления конкретного отказа телефона, а не для заявления о снятии внешнего лимита.

Сетевая проверка21сентября: новый транспорт подтвердил исходный пакет0–24 по finalized-аккаунту машины, slot501756702;1950 корректных записей,minted0. Построен неподписанный пакет1950–1974,1147байт. Настоящий simulateTransaction Devnet завершился err:null,96974compute units, slot501756927; возвращённый симуляцией аккаунт декодирован Metaplex и проверен loadedItems:1975 корректных записей. Подписей и отправок не было, фактический прогресс1950. Доказательство: docs/verification/devnet-upload-20260921.json. Один getGenesisHash в среде агента превысил15с, резерв ответил; остальные запросы заняли9–13с. Скорость и отсутствие429 на телефоне не подтверждены.

Проверки перед публикацией:93 локальных теста, включая исходник и собранный SDK, старый журнал250, отсутствие архивного запроса для записанного пакета, запрет очистки pending при устаревшем ответе/429, сохранение точного метода ошибки и очистку старой диагностики в интерфейсе. Build/stage/verify-static обязательны. Следующий конкретный шаг: проверить опубликованные хэши; для оставшегося отказа нужен текущий полный журнал из Phantom с диагностикой. Частичный журнал02:25 не заменяет текущий. Коллекцию, машину,1950записей, исходные GIF/лого/баннер и прежние два NFT сохранять. Продажи закрыты.

Публикация manual-25-check-20260921 подтверждена: commit4544e866e40e6c7fcf2c9bdc27c2bd65b6681422; Actions35566082514 и Pages35566081958 — success. После обеих сборок HTTP200 для index/controller/SDK, все три файла побайтово совпадают с локальными. SHA-256 upload SDK8a741466bfb4f787208a4ddb802d22cdf95e2cd58d7723813432d595c6610bea. Браузер подтвердил новый controller и раздел диагностики. Остаётся получить текущий журнал отказа из Phantom; реальная отправка после1950 не подтверждена.

## Текущее продолжение — 21 сентября 2026, manual-25-20260921

Подтверждена причина, по которой последние правки не доходили до телефона: опубликованный `/solana-upload/sdk.js?v=upload-manual-10` побайтово совпадал со старым SDK в Git (SHA-256 `5ec838a20c6d91045717a8ee5f8f2b002ac3b9164b9e3fdf3fb792481e08ec8d`), но не с пересобранными исходниками HEAD `fcb493e`. Этот SDK начинал проверку с getGenesisHash и finalized-чтения коллекции. Текущему исходному uploadClient.read достаточно одного confirmed getAccountInfo машины. Обновление controller/query без пересборки SDK это не исправляло.

Дополнительный воспроизведённый дефект: общий cooldown заставлял следующее ручное действие молча ждать минимум 30 секунд; интерфейс сохранял прежний текст ошибки. Внешнюю причину самого HTTP429 по этим данным установить нельзя.

Исправления: SDK и controller собраны вместе; по умолчанию нет искусственных пауз между ручными RPC. Retry-After учитывается отдельно для каждого сервера без ожидания в заблокированном интерфейсе. Разрешённые чтения используют резерв при HTTP/JSON-RPC429, сетевой ошибке, таймауте или 502/503/504. Одна попытка на сервер; sendTransaction и неизвестные методы не перенаправляются и не повторяются. Таймаут охватывает тело ответа. Неиспользуемый модуль read-runner с автоматическими повторами удалён. Проверка сразу показывает текущий статус; ответ для прежнего кошелька отбрасывается. Старый pendingGroup оставлен для безопасной сверки уже подписанных операций; новые операции по-прежнему строго одна транзакция до25 записей, только signTransaction.

Независимый finalized getAccountInfo 21 сентября, slot501745654: машина `FLpAJpBG7BDEL5cFZPiouGD6ZWnTWRRBrWxtkjVz9s3R`, коллекция `BZRkdsRsmeBBGb1JsVUbgZbThWraaMPSRazdiQdioLcy`, владелец `FNytKprG3JukM81svBhCrgHAEHht3oUgpXZFUkUbCW6y`,1950 корректных записей,minted0. Публичный снимок сохранён в tests/fixtures/upload-machine-1950.json для воспроизводимой проверки декодером Metaplex. Коллекцию и машину не создавать заново; сохранённые журналы и прогресс не очищать.

Проверки:86/86 тестов. И исходник, и собранный SDK читают снимок1950 одним RPC; тестовый провайдер получает одну подпись для1950–1974, повторное нажатие не пересылает неподтверждённый пакет. Старый журнал250 записей сверяется без новой подписи. Это локальные проверки со смоделированным кошельком, не реальные подписи Phantom. npm run build:solana, stage-site и verify-static пройдены; исходные GIF/лого/баннер проверены по SHA,10000 скрытых metadata совпадают. verify-static теперь также проверяет совпадение всех готовых SDK со staged-копиями и с HEAD в CI/--check-committed; намеренная проверка старого HEAD завершилась ошибкой, как и требуется.

Публикация подтверждена: commit `3d3734ec45991552442c3431b03721742c8aac79`, Actions `35564537762` и встроенный Pages `35564536772` завершились success. После обеих публикаций HTML, controller, оба SDK и исходный GIF получены по HTTP200 и побайтово совпали с проверенными локальными файлами. SHA-256 upload SDK: `54ebca900840e6feccd08b1de7c3a9dcf3668b87e0ed8e653f0e1dafdaaf9e4a`. Браузер загрузил controller `manual-25-20260921`; GIF на /solana-test/ загружен640×640 и совпадает с оригиналом.

Следующий этап: реальные подпись/отправка в прежнем браузере Phantom владельца. Доступа к его кошельку у агента нет; реальная загрузка после1950 не подтверждена. Публичные RPC могут ограничивать запросы (https://solana.com/docs/references/clusters); снятие внешних лимитов или100%работа на телефоне не заявляются. Продажи Mainnet закрыты. Прежние два NFT и GIF остаются на /solana-test/. Ниже — история этапов, её старые инструкции по группам и автопродолжению не применять.

## Готово
- 10000 уникальных PNG 2000×2000 из454 оригинальных слоёв; SHA исходников сохранены. Все точные и отображаемые оценки редкости уникальны. Генерацию не повторять.
- Набор изображений сохранён в private/fresh-20260918/collection.car. Отдельные дубли PNG удалены; все изображения находятся в CAR без изменения.
- 10000 финальных метаданных переведены в формат Solana. CAR полностью проверен: каждый PNG извлечён через IPFS exporter и сопоставлен по SHA и размеру, все metadata сопоставлены побайтово. SHA256 CAR: 1718812be2c8b1c964f41bf0bb1d58d4be856b4b391d2b50773899e895390b81. Размер13259774407.
- Pinata: удалены78 старых файлов, ошибок0, run35396704212. Очистительные задачи удалены из активного проекта.
- Главный сайт опубликован на Solana: commit b9252b02c491702a9e178484c9a1a48d7bbd0702; Pages35399144532 success. Phantom/Solflare, мобильный переход Phantom, исходный GIF на собственном сайте. Окно кошелька проверено в браузере.
- Баннер, лого и148-кадровый GIF на /preview/ проверены визуально. Описания без добавлений. Hidden Bear остаётся до раскрытия.
- /solana-test/ подготовлен: Devnet,2 тестовых NFT, Core Candy Machine, минт доступен только создавшему его кошельку, роялти7%, сохранение публичных адресов и восстановление. 10 локальных проверок прошли; размер каждой транзакции меньше1232байт. Это ещё не подтверждённый выпуск в сети.
- Контекст сохранён; новый приватный план и seed сохранены в libfile_80bef8a172f08191bf8f291923bbdb6e. Оригиналы в libfile_3c710cc8c4a0819185957675658a4542.

## Требует действия владельца
Открыть /solana-test/ в выбранном кошельке, подключить Solana-кошелёк и подписать тестовые операции в Devnet. Агент не знает и не запрашивает seed. Пользователь предоставил адрес владельца FNyt…CW6y и утвердил цену 0,5 SOL; параметры внесены в config.js и metadata/policy.json.
Коллекция для продажи и страница Magic Eden ещё не созданы. Утверждённая цена 0,5 SOL отображается на сайте; продажи закрыты. Баннер Magic Eden нужно проверить в реальной карточке после появления коллекции.
Открытие Pinata в браузере отклонено проверкой доступа (user declined); новую загрузку не выполнять обходным путём. Нужен новый доступ пользователя перед загрузкой.

## Следующее продолжение
Проверить подтверждённые результаты Devnet пользователя, завершить загрузку оригинальных media при восстановленном доступе, подготовить выпуск10000Core NFT с утверждённой ценой и адресом. До раскрытия не публиковать финальные PNG/metadata/CID, private/, план,seed и слои. Дата не ранее2027-01-01; стандартный Core сохраняет право обновления владельцу коллекции, сам по себе календарную блокировку не обеспечивает. Не выдавать локальные проверки за аудит или успешный запуск.
GitHub: все33 прежние ветки удалены, осталась только main. Сброс истории отклонён: GitHub Actions token не имеет workflows permission. Историю не удалось удалить; не обходить отказ. Одноразовый шаг очистки удалён. Сайт опубликован: commit85d4a61f89f889ad6def04e8a815d9a35a090c7d, deploy job105778735942 success. Новая коллекция ещё не создана в сети. Браузерная служба при финальной проверке Devnet стала недоступна; не выдавать это за успешную подпись/минт.

Исправлена гонка публикаций: встроенный Jekyll job105780730529 закончил после custom job105780561075 и убрал SDK/hidden metadata (HTTP404). Решение без изменения прав: хранить сгенерированные публичные SDK и10000скрытыхJSON также вmain; проверять их соответствие сборке. Все финальные арты сохранены приватно28частями, папка6aadb8214650819189f727245c390fc3;31сохранение успешно, исходныйZIPверсия1.

Фикс4974948618f5ac3befa1f9323b5bfa4302281936: обе публикации35401504639 и35401503459 success. HTTP-проверки после обеих публикаций прошли: /solana-test/, controller.mjs, sdk.js среальным queryпараметром, collection.json, hidden0000 и9999, исходныйGIF — все200. SDK иGIF побайтово совпадают сисходниками, MIME JavaScript корректный. Подтверждённых NFTвDevnet пока0: нужнаподписьвладельца.


## 19 сентября 2026 — выбор кошелька на телефоне

- Исправлен общий модуль подключения главной страницы и /solana-test/: Phantom и Solflare всегда представлены в окне выбора. Установленный провайдер подключается после выбора; на телефоне для отсутствующего провайдера предлагается переход в браузер соответствующего приложения, на компьютере — официальная установка. Автоматическое подключение единственного обнаруженного кошелька убрано.
- Переход Solflare реализован по официальной спецификации https://docs.solflare.com/solflare/technical/deeplinks/other-methods/browse. Обе ссылки сохраняют адрес открытой страницы. Добавлены версии URL модулей для обновления кэша. Инструкция Devnet больше не ограничена Phantom.
- Проверки: npm test — 14/14, включая выбор при единственном провайдере, отмену, повторное открытие, подключение выбранного провайдера и ссылки обоих кошельков. npm run build:solana, node scripts/stage-site.mjs, node scripts/verify-static.mjs выполнены успешно; оригиналы и 10000 скрытых metadata совпадают, продажи закрыты. Проверки окна выполнены в модели DOM; тест на реальных мобильных приложениях ещё не выполнен. Установка браузера для визуальной проверки завершиться не смогла из-за ошибок сети.
- У пользователя подключение Phantom в Devnet уже сработало, но получение тестовых SOL не завершено: faucet.solana.com отклонил GitHub как слишком новый; один прямой requestAirdrop вернул Internal error; QuickNode на скриншоте в сети Testnet потребовал баланс Mainnet. Devnet и Testnet не смешивать.
- Опубликовано в commit b1b64cb06509c844245615ca784eaee47cb56240: обе публикации Pages 35406764726 и 35406764136 успешны; 8 живых файлов совпали с проверенной сборкой. Реальные мобильные переходы ещё должен проверить владелец. Подписанного выпуска NFT пока нет.


## 19 сентября 2026 — утверждённые настройки запуска

- В config.js и metadata/policy.json внесены цена 0,5 SOL (500000000 lamports) и указанный владельцем адрес FNytKprG3JukM81svBhCrgHAEHht3oUgpXZFUkUbCW6y. Адрес хранится как ownerAddress/owner; контроль подписью ещё не подтверждён. Получатели платежей и роялти основной коллекции пока не настроены, поскольку выпуск ещё не подготовлен.
- Главная страница и калькулятор используют одну цену; русский язык показывает 0,5 SOL. Расчёт на 50 NFT — 25 SOL. Подключение кошелька не открывает покупку: кнопка минта заблокирована, demoMode:true, cluster:devnet, адреса основной коллекции и Candy Machine пустые.
- Сборка теперь проверяет согласованность цены, владельца, тиража, роялти и лимита покупки с policy.json, валидный публичный адрес и цену в целых lamports. Это проверка конфигурации, не подтверждение контроля кошелька.
- Проверки: npm test — 15/15, включая фактический app.js с переключением языков, количеством 1–50 и закрытой покупкой. npm run build:solana, stage-site, verify-static — успешно; SDK и все 10000 скрытых JSON совпадают для двух схем Pages, оригинальные публичные изображения проверены по SHA.
- Повторный одиночный запрос 1 тестового SOL через официальный Devnet RPC снова вернул -32603 Internal error без сигнатуры. Получение средств и тестовый выпуск ещё не подтверждены.
- Опубликовано в commit 0eb4999a39838f0a2afc6c20b39a8fc3d0cfc07d: обе публикации Pages 35433435259 и 35433434778 успешны, 8/8 живых файлов совпали с проверенной сборкой. Следующий шаг: получить Devnet SOL и выполнить операции на /solana-test/ с подписью владельца. Основные продажи остаются закрыты.


## 19 сентября 2026 — устранение лишнего обновления кошелька при «+»

- Пользователь сообщил, что при увеличении количества NFT дёргается элемент кошелька. Найдена лишняя связь: clamp() после каждого изменения количества вызывал renderConnectionState(), заново записывая textContent и title кнопки кошелька в шапке.
- Удалён вызов renderConnectionState() из пересчёта количества. «+», «−» и ручной ввод меняют только количество/сумму; состояние кошелька обновляется при его подключении, изменении аккаунта и смене языка. Версия app.js в index.html обновлена на quantity-20260919.
- Регрессионная проверка исполняет настоящий app.js, нажимает «+» девять раз, «−» и вводит число вручную. До исправления зафиксированы 22 лишние записи textContent/title; после исправления — 0. Цена и закрытая покупка сохранены.
- npm test — 15/15 успешно; build:solana, stage-site и verify-static успешно. Проверка выполнена в модели DOM; визуальная проверка на телефоне пользователя ещё не выполнена.
- Опубликовано в commit 961add8cfcdf73a111c2d0cbab10fdc5632817fa: custom35433843952 и Jekyll35433843473 успешны, живые index.html и app.js совпали с проверенной сборкой после обеих публикаций. Визуальный результат на телефоне ещё не подтверждён владельцем.


## 19 сентября 2026 — блокировка выпуска при пустом Devnet-кошельке

- Свежий getBalance адреса владельца: 0 lamports, confirmed, slot500789634. Один запрос меньшей суммы (0,1 тестового SOL) вернул HTTP429 Too Many Requests. После 429 повторные запросы выдачи остановлены; сигнатуры и подтверждённого поступления нет.
- Проверен devnet-pow по ссылке официального справочника Solana. CLI при начальном балансе менее5000 lamports сначала запрашивает обычный airdrop, а затем подписывает транзакцию кошельком плательщика; этот способ не решает текущий нулевой баланс. Исходник: jarry-xiao/proof-of-work-faucet, commit1efbcbf87497ed6d75a9bda373766ab11c5b4501, cli/src/main.rs. Майнинг не запускался, ключи не создавались и не запрашивались.
- /solana-test/ теперь показывает конкретный следующий шаг при балансе0 и блокирует все четыре операции выпуска при нулевом/некорректном балансе. «Проверить состояние» остаётся доступной; после положительного баланса доступны операции, разрешённые текущим этапом. Версия controller.mjs — funding-20260919.
- Проверена реальная функция render контроллера в модели DOM: 0, отрицательный, NaN, Infinity и отсутствующий баланс блокируют операции; положительный баланс разрешает подходящие этапы. npm test — 15/15; build:solana, stage-site, verify-static успешно. Это проверка интерфейса, а не подтверждение достаточности любого положительного баланса для транзакции.
- Следующее: проверить обе публикации и живую тестовую страницу. Для фактического выпуска нужны полученные Devnet SOL и подписи пользователя. Продажи основной коллекции закрыты.

## 19 сентября 2026 — текст подключённого кошелька за рамкой

- По скриншоту Phantom 13:26 подтверждено переполнение кнопки в шапке: мобильный CSS одновременно задавал max-width и white-space:nowrap, а адрес и действие выводились одной строкой.
- Адрес и действие разделены переносом строки. Кнопка использует pre-line, нормальную высоту строки, минимальную высоту54px и сжатие flex по доступной ширине; на узких экранах снята прежняя жёсткая минимальная ширина. Регистр адреса сохранён. URL app.js и mobile-fix.css обновлены до wallet-layout-20260919.
- npm test:15/15; build:solana, stage-site и verify-static успешно. Существующая проверка подтверждает отсутствие перерисовки кошелька при изменении количества. Реальные транзакции не запрашивались; цена0,5SOL, исходные изображения, скрытые metadata и закрытые продажи сохранены.
- Визуальная проверка браузером пока не выполнена: Chromium отсутствует, загрузка завершилась тайм-аутом. Следующий шаг: проверить опубликованную версию и отображение кнопки в Phantom пользователя. Общий Devnet-тест всё ещё требует тестовыхSOL и подписи владельца.

## 19 сентября 2026 — восстановление операций после потери ответа

- Продолжена работа без Devnet SOL. Номер подписанной транзакции теперь сохраняется до отправки вместе с адресом создаваемого объекта. Потерянный ответ RPC больше не оставляет операцию без сигнатуры; все повторные отправки блокируются, пока есть pending.
- Истечение blockhash больше не считается само по себе доказательством неудачи. Проверяются finalized высота/слот и getSignatureStatuses с searchTransactionHistory:true. Успешная сигнатура сохраняет блокировку до подтверждения объекта чтением сети; откат разрешён только для окончательно отклонённой операции либо отсутствующей сигнатуры после истечения срока. Устаревшие/некорректные ответы, ошибки RPC и старые файлы без сигнатуры не разрешают повторную отправку автоматически.
- Добавлены6 локальных проверок с настоящим построением и подписью тестовой транзакции в памяти и моделируемым RPC: потеря ответа, успешное подтверждение, отказ подписи, задержка чтения объекта, истечение/окончательная ошибка, устаревшие/недоступные данные. Сетевые транзакции не отправлялись.
- npm test21/21; build:solana и stage-site успешно. SDK и LEGAL сохранены в Git; verify-static успешно после индексации обновлённой сборки. Версии SDK/controller обновлены до recovery-20260919. Оригиналы, описания, цена и закрытые продажи сохранены.
- Основание проверки статусов: https://solana.com/docs/rpc/http/getsignaturestatuses и https://solana.com/docs/rpc/http/getblockheight. Это проверка поведения кода при сбоях, не подтверждение выпуска NFT или аудит контракта.
- Следующий шаг: подтвердить публикацию; реальная проверка Devnet по-прежнему требует SOL и подписи владельца. До неё основную коллекцию не открывать.

## 19 сентября 2026 — подготовлен операторский механизм раскрытия

- Добавлен solana/reveal.mjs: проверка 10000 строк сохранённой карты по SHA-256 commitment, индексов/имён/IPFS URI; привязка к инвентарю выпущенных адресов, коллекции и update authority. Оригинальные карты и изображения не публиковались и не менялись.
- Devnet-only session: предварительная сборка updateV1 без подписи, явное время не ранее2027-01-01 по finalized block time; последовательное обновление только name/URI с сохранением сигнатуры до broadcast. Продолжение читает реальные finalized аккаунты, пропускает раскрытые NFT, блокирует неизвестную операцию и повторяет только доказанно неудачную. Нет автоматического расписания или Mainnet-отправки.
- Добавлены read-only CLI scripts/preview-reveal.mjs и docs/REVEAL.md с границами готовности, требованиями к журналу и последующим минтам. Подпись/сохранение журналов подключаются оператором; единственный процесс обязателен. Внешнее durable storage и планировщик не развёрнуты.
- Локальные тесты используют синтетическую карту10000, настоящие сериализованные Core accounts и построение/подпись транзакций одноразовыми ключами в памяти. Настоящий приватный CAR в этом этапе не загружался; Devnet-транзакции не отправлялись. Защита даты программная, не on-chain запрет самой Core.
- Следующий шаг: после полученияSOL выполнить тестовый минт и подготовить отдельную безопасную репетицию раскрытия с тестовыми URI; перед основной сетью завершить hosting, инвентарь, время/подпись, обработку поздних минтов и автоматический исполнитель. Финальное раскрытие пока не настроено на автоматический запуск.
- Проверка этапа: npm test29/29, build:solana, stage-site и verify-static успешны. SDK и публичные файлы не изменились; salesOpen:false. Восемь новых тестов проверяют карту, дату, настоящие инструкции, повторный запуск, потерю ответа, задержку чтения, отмену/ошибку записи и неверные полномочия/metadata.

## 19 сентября 2026 — Blockhash not found на телефоне

- Пользователь получил2Devnet SOL (getBalance2000000000lamports, slot501026055), Testnet0. Затем при создании коллекции Phantom показал ошибку simulation Blockhash not found. Точная причина по скриншоту не установлена: истечение при подписи или отставание RPC/кошелька. Успешная коллекция пока не подтверждена.
- sendTracked запрашивает confirmed blockhash вместе с context.slot, передаёт minContextSlot и явный confirmed preflight в sendTransaction. Проверки симуляции не отключены. После подписи проверяется blockheight: просроченная транзакция не отправляется и не сохраняет адрес нового объекта. Повтор требует нового нажатия и подписи, автоматической переподписи нет.
- При ошибке после попытки отправки pending/signature сохраняются. «Проверить состояние» использует прежнюю безопасную проверку finalized expiration/status; очистка браузерных данных не нужна. Сообщение ошибки переведено и объясняет следующий шаг. Перед refresh старый снимок состояния сбрасывается, чтобы ошибка чтения не разрешала действия по устаревшим данным.
- Основание: https://solana.com/developers/cookbook/transactions/confirmation (одинаковый commitment, задержка RPC, истечение blockhash). Добавлены регрессионные тесты медленной подписи, preflight failure с блокировкой дубля, malformed RPC и сообщения пользователю. Реальный повтор на Phantom остаётся необходимым.
- Проверки:33/33 теста, build:solana, stage-site, verify-static успешно. SDK пересобран для обеих схем Pages, версии SDK/controller — blockhash-20260919. Пользователю после публикации обновить страницу, подключить кошелёк, «Проверить состояние», затем повторить разрешённый этап. Если блокировка остаётся — сохранить результат для проверки сигнатуры, не очищать состояние.

## 19 сентября 2026 — исправление getBlockHeight is not a function

- Скриншот23:24 выявил ошибку предыдущего исправления: в Umi1.6 нет rpc.getBlockHeight. Локальный тест ошибочно добавлял такой метод и не выявил несовместимость. Исправлено на реальный rpc.call('getBlockHeight', [...]); ответ проверяется как безопасное неотрицательное целое.
- Добавлен интеграционный тест настоящего Umi/web3 RPC-адаптера с подменой только HTTP-ответов и ожидания подтверждения: проходит получение blockhash, проверка высоты, сериализация и отправка подписанной транзакции. Несуществующие методы RPC в тест больше не добавляются. Реальные средства и сеть не используются.
- Версии controller/SDK:rpc-height-20260919. Подключение кошельков не менялось: браузерные переходы открывают страницу внутри Phantom/Solflare, затем пользователь подключается. Два кошелька — текущий список интеграции сайта, не ограничениеSolana. Расширение списка и мобильный адаптер в этом исправлении не реализованы.
- Проверки:34/34теста, build:solana, stage-site и verify-static прошли. Следующий шаг: после публикации обновить тестовую страницу и повторить создание с подписью пользователя; подтверждённого выпуска всё ещё нет.

## 19 сентября 2026 — результат минта и сохранение в Phantom

- Скриншот23:35 показывает подтверждённую тестовую коллекцию и остаток1,99836052SOL. Скриншот23:38 показывает CoolBears#0000 — Hidden Bear, «владение подтверждено». Это результат клиентской проверки на скриншоте; агент ещё не получилJSON/ссылку для независимой проверки конкретногоNFT.
- Phantom сообщает File downloads are not supported при скачиванииJSON. Добавлены показ результата в readonly textarea и отдельная кнопка копирования. Если ClipboardAPI недоступен/отклонён, текст остаётся видимым и выделяется, дана инструкция ручного копирования. Скачивание файла сохранено как дополнительный вариант.
- Добавлено восстановление из текста с той же проверкой, что для файла; несовпадающий подключённый кошелёк и незавершённая текущая операция не разрешают заменить состояние. Журнал pending/signature сохраняется при экспорте. Нет отправки данных третьим лицам.
- Следующий шаг: обновить /solana-test/, «Скопировать результат» и вставить текст в чат для независимой проверки NFT. Повторять уже успешный минт#0000 не нужно. Версия controller:backup-text-20260919; SDKне менялся.
- Проверки:37/37 тестов, build:solana, stage-site, verify-static успешны; тесты исполняют настоящийcontroller в моделиDOM, включая отказClipboardAPI и сохранностьpending. Проверка ручного выделения на физическомPhantom ещё нужна.

## 19 сентября 2026 — оба тестовых NFT и репетиция раскрытия

Независимое чтение Devnet подтвердило #0000 (8EQdWaor3Pj9y2J3V7a7zR1bfp2m1m5zHcsdjRtc3NQK) и #0001 (bqxPFpkcKhejMBD5YnS5LGVLefEtEhseLgn63xBhQ8n), owner FNytKprG3JukM81svBhCrgHAEHht3oUgpXZFUkUbCW6y. Коллекция 7TBBVkBziGZxGp8a8Uhv6U27fpj8gLQJP2mFE4H19dj9 содержит2NFT; машина3cCPC8tECbbqj7j6HMLuPNF8ga8YVkrdwbRn15rjTjwu выпустила2из2. Последнийминт finalized, err=null, slot501043158.

Добавлена отдельная Devnet-репетиция для этих двух фиксированных адресов. Обновляет только name и URI через Core updateV1; использует уже публичные promo-02/03, не финальные изображения соответствующих номеров. Закрытые metadata, оригинальная карта и защита даты в reveal.mjs не изменены. Проверяет владельца, authority, коллекцию, роялти7%, точное исходное/конечное имя и URI. По одной подписи на NFT; отправка через существующий журнал pending, повтор неизвестного результата блокируется. Проверка состояния распознаёт завершённое обновление после перезагрузки. На странице показываются тестовые картинки с пояснением.

38/38 локальных тестов прошли, включая настоящий updateV1 и запрет Mainnet/чужих адресов/повторного обновления/неожиданных metadata. Реальное раскрытие в Phantom ещё НЕ выполнено: следующий шаг владельца — проверить состояние, нажать «Раскрыть следующий тестовый NFT», подписать, повторить для второго. Основные продажи закрыты, автоматическое раскрытие не развёрнуто.

## 20 сентября — исправление пустого обновления при репетиции

Обе пользовательские подписи раскрытия finalized без ошибки, но прямое finalized-чтение на slot501048134 показало исходные hidden name/URI у обоих NFT. Причина: отдельный test-reveal.mjs передавал name/uri вместо newName/newUri, поэтому SDK сериализовал пустое обновление. Исправлено; операторский reveal.mjs уже использовал правильные поля. Добавлена проверка декодированных байтов настоящей инструкции: newName/newUri должны быть Some с точными значениями, newUpdateAuthority None. После отправки теперь отдельно читается и проверяется конечное состояние NFT. Надпись #0002 на отключённой кнопке заменена на «Оба тестовых NFT выпущены». Владелец/коллекция/роялти не менялись. Нужна повторная подпись владельца после публикации reveal-fields-20260919; успешного раскрытия ещё нет.

## 20 сентября — Wallet Standard и ссылки проекта

Оба Devnet NFT успешно раскрыты: finalized-чтение подтвердило конечные name/URI, прежнего владельца, коллекцию и700bps; последние подписи наslot501050092/501050174 finalized err=null. Скриншоты показывают правильное отображение в тестовой странице внутриPhantom. Нативные карточки NFT иMagicEden ещё не проверены.

Добавлен Wallet Standard через закреплённые npm-пакеты @wallet-standard/app и @solana/wallet-standard-wallet-adapter-base. Выбор показывает Phantom,Solflare,Backpack (официальная установка если не обнаружен), а также другие обнаруженные кошельки с поддержкой Devnet и legacy signTransaction. Установленные провайдеры Phantom/Solflare сохранены. Стандартные адаптеры кэшируются; изменение аккаунта обновляет подключённый адрес; регистрация/удаление кошельков обновляет открытый список. Главная страница не загружает модуль до подключения.

Мобильный переход Phantom/Solflare добавляет connectWallet с именем выбранного кошелька. На странице назначения запрашивается только подключение выбранного доступного провайдера; маркер удаляется до запроса, повторного автоматического цикла нет. Подпись транзакций отдельно. Если провайдер не обнаружен, остаётся выбор; ограничение пользовательского жеста конкретного кошелька требует ручного нажатия. Прямой MWA/WalletConnect-сеанс Chrome без браузера кошелька не внедрён.

Проверки охватывают настоящий стандартный адаптер с локальной сериализацией и подписью транзакции, сменой аккаунта/отключением, фильтрацией несовместимых кошельков, продолжением мобильного перехода. Физические Backpack/Solflare/Phantom ещё требуют пользовательской проверки.

СсылкаX @coolbears_nfts была вfooter; добавлена также вhero иtwitter:site. ДляMagicEden подготовлены поля вdocs/MAGIC_EDEN.md; реальнаякарточкаMainnetне создана, официальныйURLневыдуман. Продажиостаютсязакрыты.

Итог проверок:39/39тестов,build:solana,stage-site,verify-staticуспешны. Обе схемыPagesсодержаттакжеwallet-standard.jsиLEGAL; оригинальныепубличныеassetsи10000hiddenmetadataпроверены.

## 20 сентября — ссылка X только внизу сайта

По просьбе владельца удалена кнопка X @coolbears_nfts с первого экрана. Прежняя ссылка в footer сохранена. Подключение кошельков и другие настройки не менялись.

## 20 сентября — неподписанная подготовка Mainnet

Добавлены solana/launch-plan.mjs и scripts/preview-launch.mjs: 10000 последовательных NFT, первый минт только владельцу с ограничением общего выпуска до1, затем guards оплаты0,5SOL. Построение открытия требует finalized-проверки всех10000записей, #0000 владельца, коллекции, роялти7%, программ и guards. Получатели задаются явно; адрес владельца в расчёте остаётся предложением.

43/43теста прошли, включая сериализованные guards и все400пакетов записей. Read-only MainnetRPC: баланс0,07SOL, аккаунт машины331915байт, rent1,68677844SOL только за машину. Ничего не подписано и не отправлено. Подробности и ограничения: docs/LAUNCH.md. Следующий шаг — возобновляемый операторский загрузчик с журналом неизвестных результатов; затем проверка получателей/полного бюджета до подписи владельцем. Продажи закрыты, автоматическое раскрытие и MagicEden ещё не готовы.

## 20 сентября — восстановление загрузки после обрыва

Добавлены solana/upload.mjs, solana/upload-store.mjs и tests/upload.test.mjs. Загрузчик сверяет finalized-данные всех существующих записей, отправляет максимум25 отсутствующих записей за шаг, сохраняет pending до отправки и не повторяет неизвестную транзакцию. Явно проверяет сеть/владельца, срок подписи, metadata и отсутствие начавшегося минта. Файловый store использует atomic rename+fsync и межпроцессную блокировку. Пройден локальный сценарий10000записей/400пакетов с восстановлением между вызовами и моделированием сбоев. Mainnet/Devnet-транзакции не отправлялись.

Следующий шаг: подключить ядро к операторскому интерфейсу с кошельком и браузерным журналом, провести полную сетевую репетицию до основной загрузки. Нельзя считать локальные тесты сетевым запуском. Адреса получателей Mainnet и полный бюджет ещё требуют проверки; продажи закрыты.

Проверки этапа:51/51тест, build:solana, stage-site и verify-static успешны. Для решения об истечении срока используется высота конкретного finalized-блока; статус и аккаунт читаются не ранее его slot.

## 20 сентября — браузерная страница оператора Devnet

Добавлена отдельная /solana-upload/: подключение существующего выбора кошельков, создание новой тестовой коллекции и машины на10000записей, загрузка/проверка одного пакета за нажатие, прогресс, показ журнала текстом. Старые два NFT не используются и не изменяются. Клиент жёстко использует Devnet и кошелёк владельца. Никаких автоматических подписей/отправок при открытии.

Браузерный store использует localStorage с проверкой сохранённого текста и Web Locks для защиты вкладок; без поддержки Web Locks отправки блокируются. Создание коллекции/машины использует существующий sendTracked и журнал pending до отправки. Отдельная блокировка setup не допускает гонку создания и загрузки. Возобновление работает в том же браузере; восстановление из скопированного текста в новом браузере пока не реализовано.

53/53 локальных теста, включая отказ хранилища и параллельную вкладку. Требуется реальная репетиция в Phantom: пользователь подключает Devnet, создаёт коллекцию и машину, подписывает первый пакет; агент не может подписать от его имени. 400пакетов в сети пока НЕ выполнены. Примерная аренда машины1,69 тестовыхSOL плюс остальные расходы. Сайт продолжает держать основные продажи закрытыми.

## 20 сентября — исправлена проверка Devnet

На скриншотах01:15 правильное подключение владельца блокировалось ошибкой Wrong upload network. Причина — обрезанная константа genesis Devnet. Прямой getGenesisHash официального RPC вернул EtWTRABZaYq6iMfeYKouRu166VU2xqa1wcaWoxPkrZBG; константа исправлена. Тест адаптера теперь использует независимое буквальное значение RPC, а не константу проверяемого модуля. Сообщение ошибки переведено и поднято к кнопке проверки; cache version upload-network-2.

Настоящий uploadClient.read с провайдером, запрещающим подписи, успешно прочитал баланс1,98604412тестовыхSOL. С пустым локальным журналом коллекция/машина не заданы. Никаких подписей или отправок. Следующий шаг пользователя — обновить /solana-upload/?v=upload-network-2, проверить баланс, создать тестовую коллекцию.

## 20 сентября — групповая загрузка и автоматическая проверка

Владелец вручную загрузил 1775 записей (71 пакет). Независимый finalized read подтвердил последовательные записи и minted0. Продолжаем ту же Devnet-машину FLpAJpBG7BDEL5cFZPiouGD6ZWnTWRRBrWxtkjVz9s3R и коллекцию BZRkdsRsmeBBGb1JsVUbgZbThWraaMPSRazdiQdioLcy; ничего заново не создаём.

На /solana-upload/ добавлены группы1/5/10 транзакций (до250записей), кнопка остановки, автоматическая проверка результата и переход к следующей группе. Используется signAllTransactions существующего провайдера; при отсутствии метода — одиночная подпись с автоматической проверкой. Число подтверждений внутри окна зависит от кошелька. Автозапуск только после нажатия «Продолжить загрузку», при загрузке страницы подписей нет.

Новый загрузчик использует прежний ключ журнала и блокировку, читает старый pending и history. Все подписи группы записываются в pendingGroup до первой отправки. После обрыва — finalized-проверка записей и статусов всех подписей; неизвестный результат не пересылается, истечение/ошибка требуют нового явного запуска. Подписанные байты хранятся только в памяти; неотправленные после закрытия страницы ждут доказанного истечения. Отказ сохранения, отмена кошелька, подмена сообщения и неверная сеть останавливают отправку. Пауза не отменяет уже отправленные транзакции.

64/64 локальных теста: продолжение с1775, все оставшиеся329пакетов за33запроса групповой подписи, обрывRPC, восстановление, отмена, срок, частичный результат, старый журнал и состояния интерфейса. Локальная репетиция не равна сетевой. Следующий шаг владельца после публикации: открыть /solana-upload/?v=upload-groups-1, проверить состояние и нажать «Продолжить загрузку». Проверить настоящую групповую подпись Phantom и затем завершение10000. Основные продажи, приватные оригиналы и старые2NFT не изменялись.

## 20 сентября — ручной режим по просьбе владельца, защита от429

После первой группы Phantom пользователь получил429 Connection rate limits exceeded. Независимый finalized-read подтвердил1950 корректных записей, minted0: успели примениться175 новых. Пользователь выбрал ручное продолжение. На /solana-upload/ по умолчанию1транзакция=25записей, автоматический запрос следующей группы выключен. Одно нажатие подписывает/отправляет один пакет и автоматически проверяет его. Неопределённая старая группа сначала проверяется, повторная отправка не происходит. Группы5/10 и непрерывное продолжение доступны явно.

Добавлен общий последовательный HTTP-загрузчик: минимум1,5секмежду запросами; getGenesisHash кэшируется30сек для того же RPC/cluster, проверка владельца остаётся при каждом действии. Отключены внутренние повторы429 web3.js. При429 цикл ждёт30/60/120сек или дольше поRetry-After, затем сначала сверяет журнал; не повторяет sendTransaction. Проверки подтверждения разделены12сек. Остановка работает и во время ожидания. После5ограничений за один запуск загрузка останавливается с понятным сообщением.

Следующийшаг: открыть /solana-upload/?v=upload-paced-1 в том же Phantom,проверитьсостояние,оставить1 и выключенный автоматический режим,нажатьПродолжитьзагрузку. Создание коллекции/машины не повторять. Сетевые подписи агента отсутствуют. Изменения не устраняют внешние ограничения общего RPC; проверка настоящего телефона остаётся необходимой.

Проверка этапа:68/68тестов; проверены интервалыHTTP, Retry-After в секундах и дате, отсутствие повтораHTTP429, восстановлениепередподписью и ручная остановка послеодногопакета.

## 20 сентября — восстановление зависшей проверки, upload-recovery-2

Воспроизведён дефект интерфейса: после 429 новая проверка сохраняла старый текст ошибки и при зависшем HTTP оставляла все действия заблокированными. Теперь новая проверка сразу показывает своё состояние, рядом доступна «Отменить проверку». Смена кошелька также отменяет чтение; запоздалый ответ не может включить загрузку или заменить результат. HTTP-запрос ограничен 15 секундами, включая чтение тела ответа; вся попытка проверки — 45 секундами. Отмена/таймаут освобождают очередь и блокировку. При 429 проверка показывает отсчёт, соблюдает Retry-After и выполняет максимум три попытки; повторяется только чтение, не подпись/отправка.

Проверка коллекции и машины использует настоящие safeFetchCollectionV1/safeFetchCandyMachine закреплённых SDK вместо пары accountExists+fetch: обычное чтение требует четыре RPC-запроса вместо шести. Проверки Devnet, владельца, finalized-записей, metadata, royalties и отсутствие минта сохранены. Ключи/формат журнала и защита неопределённых подписей не изменены. Таймаут отправки оставляет всю группу в журнале и не разрешает её повторную отправку.

77/77 тестов успешны, включая реальные Umi/web3 HTTP-вызовы, зависание заголовков/тела, отмену активного/очередного запроса, освобождение блокировки, Retry-After, поздний ответ и отсутствие повторной отправки после таймаута. build:solana, stage-site и verify-static пройдены; обе схемы Pages содержат тот же новый SDK и все 10000 скрытых metadata. Исходные assets, основные продажи и старые два NFT не менялись.

Независимое read-only чтение новой реализацией подтвердило коллекцию BZRkdsRsmeBBGb1JsVUbgZbThWraaMPSRazdiQdioLcy и машину FLpAJpBG7BDEL5cFZPiouGD6ZWnTWRRBrWxtkjVz9s3R: 1950 корректных записей, minted=0, баланс владельца 0.2956272 тестовых SOL. Агент ничего не подписывал и не отправлял в блокчейн. Внешние ограничения публичного RPC этим не отменяются. Следующий шаг после публикации — проверка новой версии в прежнем браузере Phantom и ручное продолжение существующей загрузки; не создавать коллекцию/машину заново и не очищать данные браузера. Полная сетевая загрузка 10000, Mainnet и фактическое отображение Magic Eden остаются незавершёнными.
