# CoolBears — исправленный TESTNET: операции проверены, Getgems ожидает метаданные

## Актуальная точка: 2026-09-18T20:18:24.292350+00:00

Текущий пакет approved-date-only-1: `fb79d4914cf058be5f846dde840d9e8fc0f23cf1d603a913fc0ffd2f27375147`.
Коллекция TESTNET: `0QDXQnCTPsnb2fI8_CEL3TC-gKsTK_bbwPkSOPoerkcAeXLs`.
Getgems: https://testnet.getgems.io/collection/EQDXQnCTPsnb2fI8_CEL3TC-gKsTK_bbwPkSOPoerkcAeZSj

Пользователь подписал создание исправленной коллекции, получение #0000, открытие TESTNET-минта и получение #0001. Все четыре операции проверены чтением TESTNET: реальные BOC транзакций разобраны, непрерывная история коллекции проверена, отправки и поступления сопоставлены по хешам сообщений. Точная исходная StateInit совпала с пакетом. Выпущено 2/10000, тестовый минт открыт, раскрытие не выполнено. Оба NFT принадлежат закреплённому кошельку пользователя. Цена каждого 7 тестовых TON и возвраты проверены. Отчёт: release/current-testnet-transaction-audit.json, checkedAt 2026-09-18T20:12:53.928Z. Это данные Toncenter, не независимое доказательство lite-client.

Повторно скачаны публичные collection.json, 0000.json, 0001.json и GIF. JSON совпали с утверждёнными локальными файлами: три согласованных абзаца коллекции, дата без конкретного времени в NFT, атрибутов и рангов нет. GIF совпал по SHA256, все148кадров декодируются, длительность4950мс. Отчёт: release/current-testnet-prereveal-media.json. Это проверка исходных файлов, а не рендера Getgems.

Getgems видит2NFT, но показывает Failed to Load Metadata/Metadata Unavailable для коллекции и обеих карточек. Нажатие Refresh Metadata для коллекции прошло; показан ответ Refreshing Metadata / It may take a few minutes. Затем автоматическая проверка разрешений отклонила первое нажатие Refresh у отдельного NFT, потребовав явного разрешения пользователя на внешнее обновление. Поэтому индивидуальные запросы не выполнены; не повторять их без такого разрешения и не обходить отказ через API. Можно продолжать чтение и проверку. В следующем ответе объяснить отказ автопроверки и запросить разрешение только на Refresh Metadata у #0000 и #0001. Сам минт повторять не нужно.

MAINNET остаётся HOLD: testnetVerified=false до завершения Getgems, mainnetVerified/creatorNftVerified/publicMintApproved/automaticRevealArmed=false. Все68тестов защиты запуска прошли. MAINNET-платежей, новых транзакций от агента, раскрытия или публикации финальных приватных файлов не было.

Перед этим проверена производительность главного сайта и опубликован commit b949bf2f1243358b89ed11007dd3e5ee64b21df7: главное изображение fetchpriority=high, GIF и6картинок галереи loading=lazy, все картинки decoding=async и фиксированные пропорции. Байты иURL всех изображений, тексты, контракты не менялись. Pages run35389141415 success. Браузер подтвердил отложенную загрузку галереи (~1,25МБ), загрузку всех6изображений после прокрутки, квадратные пропорции и отсутствие горизонтального переполнения; EN/RU/ZH, счётчик, FAQ и кнопка наверх работают. Полноценный мобильный Lighthouse/INP не измерялся. Большую часть времён curl занимало соединение проверочной среды; не выдавать их за скорость сайта у пользователя.

Пользователь уточнял именно слова Hidden Bear: в финальных JSON #0000/#0001 названия без этой приписки; она исчезает после раскрытия и обновления метаданных. Убирать её до раскрытия пользователь не просил.

Эта актуальная запись заменяет противоречащие ей старые статусы ниже. Не повторять развёртывание или тестовый mint для восстановления контекста.

---

# 18 сентября 2026 — исправление публичного описания

Пользователь требует убрать публичные упоминания зарезервированного NFT, в особенности абзац на сайте. Восстановлен исходный утверждённый текст из трёх абзацев в `metadata/collection.json`, без добавлений. Удалён блок со страницы сайта и из EN/RU/ZH переводов, скорректированы подписи служебных страниц.

Getgems текущей TESTNET-коллекции уже показывает оба GIF, баннер, логотип, название и описание; ранги и атрибуты скрыты. **Но отображаемое описание было отклонено пользователем.** Это не готовый релиз. Текущий контракт не имеет функции произвольного изменения URI, а IPFS-файл неизменяем. Обновление страницы Getgems не заменяет закреплённый текст.

Текущий установленный пакет `385d5550…` остаётся HOLD; `testnetVerified=false`, `metadataCorrectionPending=true`. Ни один существующий контракт не менялся, новые транзакции не отправлялись. Публичный сайт может обновиться независимо от этого.

Отдельно подготовлен, но НЕ установлен и НЕ развёрнут исправленный неподписанный пакет: `release/description-correction/unsigned-candidate.json`, SHA256 `f4fb17938415afa7541e69463337565a0e3a0e7c73a1eb71d3b87582c8becd05`. Исправлены описания до и после раскрытия. Финальные 10000 PNG и 10000 индивидуальных JSON прежние. Полностью проверен исходный CAR и его73617блоков; изменены только collection JSON и корневой блок. 13 локальных TVM-проверок раскрытия прошли. Новый CAR SHA256 `7fd7eae76d315db216e0dbfb08bec036b1d0d80c4aa8d84a7472110de0efa853`; это локальная подготовка, не подтверждение публикации или нового TESTNET.

Публичная IPFS-публикация действующего пакета приостановлена флагом коррекции, чтобы не смешивать исправленный исходный JSON со старой закреплённой ссылкой. Новые метаданные в IPFS не закреплены, новый приватный CAR ещё не перенесён в Pinata, новый адрес не проходил live TESTNET. Не выдавать старые доказательства за проверку нового пакета. Последнее уточнение пользователя «на сайте» прежде всего требует немедленно опубликовать исправленный сайт, не запускать новый контракт.

---

## Более ранние контрольные точки (могут быть частично устаревшими)

# CoolBears — текущий неподписанный выпуск, 18 сентября 2026

Текущий пакет — `final-collection-branding-1`, SHA256 `385d5550ff4cb83422e2ff052e958225aa859729f9ba7adfb0a7a04825977e2c`.
Будущий MAINNET-адрес: `UQD3XsAkackNZuBbHjYj0UKJjVz-aMxzGHA70b4MxY7i9vYL`.
Это **не разрешение развёртывать или переводить TON**. Фактические права задаются только проверенным `release/launch-state.json`; сохраняется HOLD.

Исправлено оформление после раскрытия: финальные `image` и `cover_image` теперь используют утверждённые логотип и баннер. Все10000 индивидуальных PNG/JSON побайтно прежние; номера, характеристики, ранги, рабочий GIF, цена, владелец и royalty не менялись. Manifest SHA256 `208cdabdfa3ed6f4b01d23fbcec076b22cd6da5c582c6bef9d3b7c43f9b04488`; CAR SHA256 `9b419a1d4642fd3e3b7d7ddcb1f27db6e25532b756634165a269ae0b76b02161`.

Полный CAR собран и проверен;13 сценариев фактического раскрытия в локальной TVM,66 проверок защиты запуска,22 сценария текущего StateInit и14 тестов аудитора прошли. Скрипты миграции воспроизвели тот же полный CAR двумя независимыми путями. Финальные URI и reveal preimage не публиковались. Сводка локального этапа — `release/final-branding-local-proof.json`.

Приватная миграция успешно завершена: https://github.com/etovova/coolbears/actions/runs/35363998313 . Все48 частей сверены, полный CAR SHA256 совпал. Загружены только две новые части1/48,46 средних частей использованы повторно; старые объекты не удалены. Свежая квитанция записана в release-state. Запрос миграции отключён, повторный автоматический запуск убран.

Новый адрес ещё не проходил live TESTNET. Нужны отдельное разрешение владельца и проверка текущей коллекции в Getgems: анимированный GIF, правильное оформление, отсутствие attributes/процентов/rank до раскрытия. Старые четыре диагностических NFT с R1–4 этого не подтверждают. MAINNET deploy, creator claim, открытие mint и включение автораскрытия не разрешены этим этапом.

---

# Историческая точка до исправления очков и финального оформления

Обновлено 17 сентября 2026 года после проверки восстановления из Pinata. Это состояние проекта, а не восстановленная переписка. Разрешение операций определяется `release/launch-state.json`; сейчас продажи остаются на паузе подготовки (`hold`).

## Подтверждено

**Пакет v3.** `launch/candidate.json`, SHA-256 `10aff272f0be8b315f7c280539ed49483c7bdf2f7a545863064d29fffcb008fa`. Изображения, атрибуты и распределение редкости на этом этапе не изменялись.

**TESTNET завершён.** Получены #0000 и #0001. История четырёх операций, отправки кошелька, доставка NFT, цена и возвраты сверены по сообщениям транзакций: https://github.com/etovova/coolbears/actions/runs/35223414205 . Не повторять тестовый минт. Пользователь подтвердил движение GIF при прямом открытии текущего IPFS-файла; статичное превью Tonviewer не является основанием заменять GIF.

**Приватный резерв Pinata загружен полностью.** 153 849 791 байт, четыре зашифрованные части. Первая часть использована повторно, добавлены только три отсутствующие. Все части скачаны, расшифрованы и проверены по контрольным суммам; SHA-256 собранного архива совпал с исходником: `00c983a1af436b60008505ad3cd706615bd5a3e01cf1186a7127cbdde69f3252`. Успешная загрузка: https://github.com/etovova/coolbears/actions/runs/35236851867 .

**Восстановление проверено отдельным запуском.** Из Pinata повторно восстановлен весь архив; проверены 454 исходных слоя, 10 000 JSON, 70 000 записей частот, проценты и точные места. Подтверждены 10 000 разных точных и отображаемых баллов; #0000 включает единственный самый редкий вариант в каждой категории. Манифест изображений совпадает с исходной v3. Приватные финальные адреса соответствуют хешу, зафиксированному в проверенном пакете. Результаты: https://github.com/etovova/coolbears/actions/runs/35238018832 . Расшифрованные данные не включены в публичные артефакты.

**Ошибка сравнения размеров закрыта.** Pinata сообщала для первой части 41 951 050 байт, тогда как длина сохранённого и скачанного содержимого — 41 943 040 байт. Целостность подтверждена фактическими байтами и хешами, а не равенством этих разных значений.

**Материалы сохранены вне чата.** Полная зашифрованная квитанция находится в `release/pinata-recovery-complete.sealed.json`, сводка — `release/pinata-recovery-status.json`. Квитанция не зависит от 30-дневного срока хранения артефактов Actions. Исходная резервная копия ранее сохранена в Dropbox: `/CoolBears-private/CoolBears_v3_release_recovery_PRIVATE.zip`.

## Что пока НЕ завершено

153,85 МБ — комплект восстановления: исходные слои, точный план, метаданные, упаковщик и неподписанные пакеты. Это **не все 10 000 готовых PNG**. Полное хранение готовых изображений, запуск MAINNET и автоматическое раскрытие не подтверждены. `privateStorageVerified`, `mainnetVerified`, `publicMintApproved` и `automaticRevealArmed` не включались на основании одной резервной копии. Финальные изображения, таблицы и корневой CID этим переносом публично не раскрывались.

## Следующая последовательность

1. Восстановить готовые PNG и IPFS-пакет детерминированно из сохранённой v3, сверить все хеши с манифестом и завершить долговременное хранение всего выпуска, сохраняя приватность до раскрытия. Повторно загружать компактный резерв не требуется.
2. Перевести основной клиент на точный финальный пакет. Пока `mainnet/owner/deployment.json` и основной клиент относятся к прежнему пакету и заблокированы. Старый пакет не разворачивать.
3. После подготовки выполнить подписываемое владельцем развёртывание MAINNET на паузе, получение настоящего #0000 и независимую проверку состояния, платежей и медиа. Только затем явно разрешить продажи.
4. Настроить процесс раскрытия не раньше 1 января 2027, 00:00 UTC. Контракт допускает только заранее зафиксированные финальные метаданные один раз; планировщик ещё не включён. Это не автоматическое открытие продаж.

## Работа с секретами

Секрет кошелька не нужен и не запрашивается. Квитанция зашифрована ключом, производным от текущего `PINATA_JWT`; перед ротацией API-секрета требуется перешифровать квитанцию либо обеспечить отдельное восстановление из Dropbox. Не выводить секреты, расшифрованную квитанцию, временные ссылки и финальные CID в публичные логи или репозиторий. Старый сценарий первоначальной загрузки отключён, чтобы не создавать дубликаты.
