# Continue after PR45: explicit buyer expiry review

2026-09-23. Base PR45 commit937dda211c40b9672b01e7a48190bcb08e97d44a;
branch buyer-expiry-review-20260923. This is the commit-time checkpoint.

Implemented explicit sender.reviewExpiry({authorizeExpiryReview:true}) through
fixed HTTPS /api/buyer/review-expiry. Original SQLite anchor, finalized lifetime,
bounded fee-payer history crossing the original block, absent receipt/status,
absent finalized asset and empty asset history are mandatory. Typical review14
read calls. No signing, refresh, simulation, send or retry authorization.

Permanent server expiry evidence commits/read-backs before response. Restores
survive restart/lost acknowledgment without RPC and rebind to the current order
revision. Old preparation/quote/send records stay intact. Retired bytes cannot
prepare/check/send even when no send claim previously existed.

Browser schema2 adds a strict expiry-reviewed signing record atomically with the
reconcile event/order. Saved bytes/keys/approval/claims/history remain unchanged.
CAS rejects stale HTTP replies; rollback and lost successful replies preserve
correct outcome. Historical response remains readable and expired recovery is
idempotent after restart. See orders/EXPIRY_REVIEW.md for limits and trust model.

Local:53 Node regression cases passed,0fail,8754.783978ms;11 are new expiry tests.
Actual workerd/SQLite:10 submission scenarios,126 intercepted RPC,2 fixture
submissions,0 network transactions. Browser bundle3436305bytes compiles; syntax
and whitespace checked. Local Chromium unavailable. Full exact-head CI/browser
results remain pending at commit time; record final results in PR and the
existing authoritative continuation without another result-only commit.

New Chromium coverage:7 scenarios within the linked submission runner, including
full browser/server restart, incomplete history, lost HTTP/local acknowledgments,
atomic evidence failure, stale paused order, altered result, retained send claim.
No real wallet/physical phone/live RPC coverage is claimed.

Next: reviewed replacement with append-only attempts and fresh original hash,
quote/consent; unresolved unsigned/partial/response-missing and failed attempts;
subsequent items, custody recovery policy, reviewed purchase UI, private setup.
The generic offline retry state does not bypass permanent browser/server claims.

Sales closed, price0.2SOL. Do not merge/deploy/open sales/request real signatures
or send transactions. Lab2/2 complete: do not repeat. Keep real journals, custody,
bundles, SQLite, locks, site/DNS/protections, RPC secrets/CORS/ledger/Helius plan,
10000PNG/454layers/GIF/logo/banner and private metadata intact. Owner password
only in owner's safe localTTY; never in chat or generated by the assistant.
