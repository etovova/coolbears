# Продолжение PR35: последовательная очередь владельца

23 сентября 2026. Проверена точка остановки: PR35 `6758e96c`, CI 35876357593
завершён успешно. Ветка этого изменения: `deployment-owner-queue-20260923`.
Окончательные PR/HEAD и CI сохраняются в `CoolBears_Collection(1).md` и описании PR.
На момент этого commit новый CI ещё не запущен.

## Изменение

- `queue.mjs` строит read-only progress/next action из проверенного canonical
  bundle. `owner-console/cli.mjs status` не требует пароля или RPC. В отчёт
  не входят filesystem path, endpoint/token, vault или transaction bytes.
- `prepare-next` выбирает ровно один ещё не начатый шаг; retry и активная
  попытка останавливаются до пароля. После пароля handoff сверяет сохранённые
  manifest/revision/head до vault unlock/RPC; дальнейшие preflight/simulation/CAS
  остаются прежними. Смена попытки во время ввода не обходит выбор пользователя.
- Локальная панель показывает verified-прогресс и фактическое журнальное состояние.
  Исправлена прежняя надпись «не отправлялась» при обновлении уже принятой операции.
  Подписи и accepted не увеличивают завершённый прогресс.
- Подсказки восстановления не освобождают wallet claims и не запускают reviews.
  Один pinned request на панель. Групповой подписи и автоматической отправки нет.

## Проверка на момент commit

- Новый queue suite: 4 passed, 0 failed, 98558 мс, `/tmp/pr36-queue.log`.
  Реальные временные bundle/journal/crypto, синтетический TTY, перехваченный RPC.
  Проверены restart, утрата ответа кошелька, stale selection во время пароля,
  редактирование fixture READY, приватность отчёта, сохранение истории и Devnet-only.
- Связанный sender → accepted → finalized verified → следующий шаг: 1 passed,
  50966 мс, `/tmp/pr36-linked.log`. Сетевые ответы синтетические, live sends=0.
- Проверки существующего handoff/owner-console продолжаются в
  `/tmp/pr36-handoff.log`; итог фиксируется в актуальной точке/PR.
- Локальная сборка owner-console и `git diff --check` пройдены.
- В текущем контейнере Chromium не установлен; прежняя загрузка браузера не
  прошла. Новый browser case проверит CI с собственной установкой Chromium.
  Снимки/тестовые кошельки не подтверждают реальные Phantom/Solflare/телефоны.

## Границы и дальше

Реальные custody/password/endpoint/secrets по-прежнему не настроены. Пароль
задаёт владелец в своём локальном TTY; его не генерировать вместо владельца и
не просить в чате. Для нового реального deployment нужны отдельный приватный
gateway и свежие оценки/симуляция. Групповая подпись, buyer RPC/покупка 1–50 и
реальные кошельки/телефоны остаются незавершёнными.

Продажи закрыты, 0,2 SOL; Mainnet/открытие отдельно. Лабораторию 2/2 не повторять.
Ни реальных ключей/подписей/транзакций, ни слияния/деплоя этим этапом нет.
Не менять lab RPC, его secrets/CORS/ledger, Helius-план, сайт/DNS/защиту и
оригиналы. Журналы, bundle, SQLite, locks и browser storage не очищать.
Приватные материалы коллекции и детали распределения не публиковать.

Инструкция: `operator/deployment/QUEUE.md`.
