name: CoolBears TON Sandbox

on:
  push:
    branches: [main]
    paths:
      - 'contracts/src/**'
      - 'contracts/tests/**'
      - 'contracts/TON_REFERENCE.lock'
      - 'scripts/reserve-rarest.mjs'
      - '.github/workflows/contract-sandbox.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sandbox:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Checkout pinned official TON reference
        shell: bash
        run: |
          source contracts/TON_REFERENCE.lock
          git clone --filter=blob:none https://github.com/${source_repository}.git /tmp/token-contract
          git -C /tmp/token-contract checkout --detach "$source_commit"
      - name: Install TON test dependencies
        run: npm install --no-save @ton-community/func-js @ton/sandbox @ton/core
      - name: Run TON Sandbox mint and boundary tests
        run: node contracts/tests/sandbox.mjs
      - name: Run TON Sandbox reveal lifecycle test
        run: node contracts/tests/reveal-sandbox.mjs
      - name: Verify unique creator rarity assignment
        run: node contracts/tests/creator-ranking.test.mjs
      - name: Profile NFT deploy reserve and fees
        run: node contracts/tests/gas-profile.mjs

# pre-mint readiness verification: 2026-09-14
