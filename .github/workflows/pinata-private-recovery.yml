name: Legacy private recovery upload (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-24.04
    steps:
      - name: Do not duplicate the completed backup
        run: echo 'Legacy upload retired. Use private-recovery-restore.yml for read-only verification of the completed four-part backup. No upload is performed.'
